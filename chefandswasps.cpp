#include<bits/stdc++.h>
using namespace std ;
typedef long long int lli ; 

bool sorter(pair<lli , int > a , pair<lli, int> b){

int main(){
    int T ;
    cin>>T ;
    int n ; 
    while(T--){
        cin>> n ;
        vector<lli> a(n) , b(n); 
        for(int i = 0 ; i < n ; i ++)
            cin>>a[i] ; 
        for(int i = 0 ; i < n ; i ++)
            cin>>b[i] ; 
 
}
