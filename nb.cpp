#include<bits/stdc++.h>
using namespace std ;
int main(){
    int n_t ;
    cin>>n_t ;
    while(n_t--){
         //char a = {'@', '#', '$', '%', '^', '&', '~', '*', '!'}.
         char arr[] = {'!', '#', '$', '%', '&', '*', '@', '^', '~'};
         for(int i = 0 ; i < n ; i++){
         }
    }
}
